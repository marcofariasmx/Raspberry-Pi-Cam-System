# Latest Dependencies for Pi Camera Streaming Application (Updated Aug 2025)

# Core web framework and server
fastapi==0.116.1
uvicorn[standard]==0.35.0

# Performance optimization for asyncio (2-4x speed improvement)
uvloop==0.21.0

# Template engine and configuration
jinja2==3.1.6
python-dotenv==1.1.1

# Optional: Additional performance packages
# Uncomment for even better performance on Pi 5, may be too heavy for Pi Zero 2 W
# httpx==0.27.2        # For async HTTP client if needed
# orjson==3.10.7       # Faster JSON serialization

# Camera libraries 
picamera2==0.3.30

# Note: libcamera system integration handled automatically by setup.sh
# System dependencies: libcap-dev libcamera-dev python3-libcamera libjpeg-dev

