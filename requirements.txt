# Minimal dependencies for Pi Camera Streaming Application

fastapi==0.115.12
uvicorn==0.34.2
jinja2==3.1.6
python-dotenv==1.0.1

# System packages (installed via apt, not pip):
# python3-picamera2
# python3-venv